# Test generated by RoostGPT for test roostPostmanTest using AI Type Azure Open AI and AI Model roost-gpt4-32k

# RoostTestHash=a64e3feb39

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "logout_get_success.csv"
    fields:
      - "variable1"
      - "variable2"
    skipHeader: true

scenarios:
  - name: "Logout Scenario"
    flow:
      - log: "Logout request"
        get:
          url: "/user/logout"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - hasStatusCode: 200
            - hasHeader: "Content-Type"
            - matchJSON: $.message, "successful operation"
        think: 1
