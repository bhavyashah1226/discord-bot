# Test generated by RoostGPT for test roostPostmanTest using AI Type Azure Open AI and AI Model roost-gpt4-32k

# RoostTestHash=da281fe471

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "./user_username_get_others.csv"
    fields:
      - "username"
    skipHeader: true
  phases:
    - duration: 60
      arrivalRate: 20

scenarios:
  - name: "Testing /user/{username} endpoint for fail cases"
    flow:
      - think: 5
      - log: "Testing for invalid URL"
      - get:
          url: "/users/{{ username }}"
          capture:
            - json: "$.properties"
              as: "responseBody"
          expect:
            - statusCode: 404
            - hasProperty: "responseBody"
      - think: 5
      - log: "Testing for a non-existent username"
      - get:
          url: "/user/nonexistentUsername"
          capture:
            - json: "$.properties"
              as: "responseBody"
          expect:
            - statusCode: 404
            - hasProperty: "responseBody"
      - think: 5
      - log: "Testing for invalid Auth"
      - get:
          url: "/user/{{ username }}"
          headers:
            Authorization: "Invalid Auth Token"
          capture:
            - json: "$.properties"
              as: "responseBody"
          expect:
            - statusCode: 401
            - hasProperty: "responseBody"
