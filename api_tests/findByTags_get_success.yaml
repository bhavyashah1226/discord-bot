# Test generated by RoostGPT for test roostPostmanTest using AI Type Azure Open AI and AI Model roost-gpt4-32k

# RoostTestHash=c43742dac4

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "findByTags_get_success.csv"
    fields:
      - tags
    skipHeader: true

scenarios:
  - name: "Find pets by tags test scenario"
    flow:
      - log: "Initiating API request to find pets by tags."
      - get:
          url: "/pet/findByTags"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          qs:
            tags: "{{ tags }}"
          expect:
            - statusCode: 200
            - hasProperty: "name"
            - hasProperty: "photoUrls"
      - log: "API request for finding pets by tags has been successful."
      - think: 1
