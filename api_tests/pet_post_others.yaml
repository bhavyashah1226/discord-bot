# Test generated by RoostGPT for test roostPostmanTest using AI Type Azure Open AI and AI Model roost-gpt4-32k

# RoostTestHash=16cf30f894

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  payload:
    path: "pet_post_others.csv"
    skipHeader: true
    fields:
      - "id"
      - "name"
      - "photoUrls"
      - "status"
  plugins:
    expect: {}

scenarios:
  - flow:
      - log: "Case: Invalid URL"
      - post:
          url: "/pets"   # Invalid URL
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            Content-Type: "application/json"
          json:
            id: "{{ id }}"
            name: "{{ name }}"
            photoUrls: ["{{ photoUrls }}"]
            status: "{{ status }}"
          expect: 
            - statusCode: 404    #Expecting a 404 Not Found error.
      - think: 2

      - log: "Case: Request Body Missing"
      - post:
          url: "/pet"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            Content-Type: "application/json"
          json: {}  #Empty request body
          expect: 
            - statusCode: 405
      - think: 2

      - log: "Case: Request body has incorrect data type for id"
      - post:
          url: "/pet"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            Content-Type: "application/json"
          json: 
            id: "InvalidId"  # Invalid data type for id
            name: "{{ name }}"
            photoUrls: ["{{ photoUrls }}"]
            status: "{{ status }}"
          expect: 
            - statusCode: 405  
      - think: 2

      - log: "Case: Authorization Failed"
      - post:
          url: "/pet"
          headers:
            Authorization: "InvalidToken"   # Invalid Authorization token
            Content-Type: "application/json"
          json: 
            id: "{{ id }}"
            name: "{{ name }}"
            photoUrls: ["{{ photoUrls }}"]
            status: "{{ status }}"
          expect: 
            - statusCode: 401  # Expecting a 401 Unauthorized error.
      - think: 2

      - log: "Case: Invalid Headers"
      - post:
          url: "/pet"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            Content-Type: "application/text"   # Invalid content-type
          json: 
            id: "{{ id }}"
            name: "{{ name }}"
            photoUrls: ["{{ photoUrls }}"]
            status: "{{ status }}"
          expect: 
            - statusCode: 415  # Expecting a 415 Unsupported Media Type error.
      - think: 2
