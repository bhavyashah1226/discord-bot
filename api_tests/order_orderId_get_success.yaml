# Test generated by RoostGPT for test roostPostmanTest using AI Type Azure Open AI and AI Model roost-gpt4-32k

# RoostTestHash=74f49e1ef7

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
      rampTo: 50
  payload:
    path: "order_orderId_get_success.csv"
    fields: ["orderId"]
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Fetch order by Id"
    flow:
      - get:
          url: "/store/order/{{ orderId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          beforeRequest: "logRequest"
          expect:
            - statusCode: 200
            - hasProperty: "id"
            - hasProperty: "petId"
            - hasProperty: "quantity"
            - hasProperty: "shipDate"
            - hasProperty: "status"
            - hasProperty: "complete"
            # Order properties should match the documented schema
      - think: 1
