# Test generated by RoostGPT for test roostPostmanTest using AI Type Azure Open AI and AI Model roost-gpt4-32k

# RoostTestHash=354b987cbe

config:
  target: "{{ $processEnvironment.API_HOST }}"
  processor: "./my_processor.js"
  plugins:
    expect: {}
  payload:
    path: "uploadImage_post_success.csv"
    fields:
      - "id"
      - "additionalMetadata"
      - "file"
    skipHeader: true
    order: sequence

scenarios:
  - name: "Upload image to pet"
    flow:
      - post:
          url: "/pet/{{ id }}/uploadImage"
          beforeRequest: "setHeaders"
          headers:
            authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          formData:
            additionalMetadata: "{{ additionalMetadata }}"
            file: "{{ file }}"
          expect:
            - statusCode: 200
            - hasProperty: "message"
            - hasProperty: "code"
            - hasProperty: "type"
          log: "Post request to upload image API"
        think: 1
