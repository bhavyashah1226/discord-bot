# Test generated by RoostGPT for test roostPostmanTest using AI Type Azure Open AI and AI Model roost-gpt4-32k

# RoostTestHash=a66f7079af

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "inventory_get_success.csv" 
    skipHeader: true
    fields: 
      - "var1"
      - "var2" 
scenarios:
  - name: "Testing /store/inventory API"
    flow:
      - log: "Fetching the inventory"
        get:
          url: "/store/inventory"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "object"
            - hasHeader: "Authorization"
          think: 1

