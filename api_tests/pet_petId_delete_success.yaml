# Test generated by RoostGPT for test roostPostmanTest using AI Type Azure Open AI and AI Model roost-gpt4-32k

# RoostTestHash=fbd3e66b3b

config:
  target: "{{ $processEnvironment.API_HOST }}"
  overrideHeaders:
    Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "./pet_petId_delete_success.csv"
    fields:
      - "petId"
    skipHeader: true

scenarios:
  - name: "Testing pet API"
    flow:
      - log: "Starting test for delete pet API"
      - post:
          url: "/pet/{{ petId }}"
          headers:
            api_key: "YOUR_API_KEY"
            Content-Type: "application/x-www-form-urlencoded"
          beforeRequest: "setPetData"
          expect:
            - statusCode: 200
            - hasProperty: "message"
      - think: 1
