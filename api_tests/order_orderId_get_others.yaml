# Test generated by RoostGPT for test roostPostmanTest using AI Type Azure Open AI and AI Model roost-gpt4-32k

# RoostTestHash=74f49e1ef7

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "order_orderId_get_others.csv"
    fields:
      - "orderId"
    skipHeader: true
scenarios:
  - name: "Order API failure cases"
    flow:
      - log: "Sending request with non-integer orderId"
        get:
          url: "/store/order/{{ orderId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
        expect:
          - statusCode: 400
          - hasProperty: 'error'
          - equals: 
              - "{{ response.headers['content-type'] }}"
              - "application/json; charset=utf-8"
        # Non-integer orderId should return a 400 error with a corresponding error message
      - think: 1
      - log: "Sending request with invalid URL"
        get:
          url: "/store/invalidUrl/{{ orderId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
        expect:
          - statusCode: 404
        # Invalid URL should return a 404 status code
      - think: 1
      - log: "Sending request with unauthorized token"
        get:
          url: "/store/order/{{ orderId }}"
          headers:
            Authorization: "invalid_token"
        expect:
          - statusCode: 401
        # Invalid token should return a 401 Unauthorized error
