# Test generated by RoostGPT for test roostPostmanTest using AI Type Azure Open AI and AI Model roost-gpt4-32k

# RoostTestHash=d8125b8dc9

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payloads:
    - name: "user_username_delete_others.csv"
      path: "user_username_delete_others.csv"
      fields: ["username"]
      skipHeader: true
scenarios:
  - name: "Delete User API Test Scenario"
    flow:
      - log: "Invalid username supplied - Non-existing username is provided"
        delete:
          url: "/user/{{ username }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "description"
        think: 1
      - log: "User not found - Username is missed in the path"
        delete:
          url: "/user/"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "description"
        think: 1
      - log: "Unauthorized - Invalid authorization token is provided"
        delete:
          url: "/user/{{ username }}"
          headers:
            Authorization: "Invalid_Token"
          expect:
            - statusCode: 401
        think: 1
      - log: "Invalid request URL"
        delete:
          url: "/invalidURL/"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
        think: 1
