# Test generated by RoostGPT for test roostPostmanTest using AI Type Azure Open AI and AI Model roost-gpt4-32k

# RoostTestHash=b50615cd0e

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}

scenarios:
  - name: "Failure and Error Testing for /user/createWithArray"
    flow:
      - log: "Attempting to POST without necessary body"
        post:
          url: "/user/createWithArray"
          headers:
            Content-Type: "application/json"
            Accept: "application/json"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400 # expecting a bad request response because of no body
            - hasProperty: "error"
        # after request, simulating user think time of 5 seconds
        think: 5

      - log: "Attempting to POST with empty user list"
        post:
          url: "/user/createWithArray"
          json:
            body: []
          headers:
            Content-Type: "application/json"
            Accept: "application/json"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400 # expecting a bad request response because of empty body
            - hasProperty: "error"
        think: 5

      - log: "Attempting to POST with invalid auth token"
        post:
          url: "/user/createWithArray"
          json:
            body: "user1"
          headers:
            Content-Type: "application/json"
            Accept: "application/json"
            Authorization: "Invalid token" # passing invalid auth token
          expect:
            - statusCode: 403 # expecting forbidden because of invalid auth token
            - hasProperty: "error"
        think: 5

      - log: "Attempting to POST to invalid endpoint"
        post:
          url: "/user/createWithArrayInvalid" # passing invalid url
          json:
            body: "user1"
          headers:
            Content-Type: "application/json"
            Accept: "application/json"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404 # expecting not found because url is invalid
            - hasProperty: "error"
        think: 5
