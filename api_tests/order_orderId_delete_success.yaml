# Test generated by RoostGPT for test roostPostmanTest using AI Type Azure Open AI and AI Model roost-gpt4-32k

# RoostTestHash=6e4c4bbafe

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "order_orderId_delete_success.csv"
    fields:
      - "orderId"
    skipHeader: true 

scenarios:
  - name: "Delete purchase order by ID"
    flow:
      - delete:
          url: "/store/order/{{ orderId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          beforeRequest:
            - log: "Delete request for order ID {{ orderId }}"
          expect:
            - statusCode: 200
            - hasHeader: "content-type"
          think: 1
        # comment: "This request should delete a purchase order with a specific order ID successfully and return a 200 status code"

      - delete:
          url: "/store/order/999999"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          beforeRequest:
            - log: "Attempting to delete order with invalid ID 999999"
          expect:
            - statusCode: 404
          think: 1
        # comment: "A delete attempt with an invalid order ID should return a 404 not found error"

      - delete:
          url: "/store/order/{{ orderId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          beforeRequest:
            - log: "Delete request again for order ID {{ orderId }} d to confirm deletion"
          expect:
            - statusCode: 404
          think: 1
        # comment: "A second delete request for the same order ID should return a 404 error since the order should have been successfully deleted"

      - delete:
          url: "/store/order/abcd"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          beforeRequest:
            - log: "Attempting to delete order with non-integer ID 'abcd'"
          expect:
            - statusCode: 400
          think: 1
        # comment: "A delete attempt with a non-integer order ID should return a 400 invalid ID supplied error"
