# Test generated by RoostGPT for test roostPostmanTest using AI Type Azure Open AI and AI Model roost-gpt4-32k

# RoostTestHash=354b987cbe

config:
  target: "{{ $processEnvironment.API_HOST }}"
  processor: "./some-processor.js"
  payload:
    path: "./uploadImage_post_others.csv"
    fields:
      - "petId"
    skipHeader: true
  phases:
    - duration: 60
      arrivalRate: 10
  plugins:
    expect: {}
scenarios:
  - name: "Upload image negative cases scenario"
    flow:
      - log: "Sending POST with invalid petId"
        post:
          url: "/pet/9999/uploadImage"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          formData:
            file:
              filePath: "dummy-image.jpeg"
              fileName: "test-image"
            additionalMetadata: "Metadata for Invalid PetId"
          expect:
            - statusCode: 404 # petId not found
            - hasProperty: "message"
      - think: 1
      - log: "Sending POST with missing additional Metadata"
        post:
          url: "/pet/{{ petId }}/uploadImage"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          formData:
            file:
              filePath: "dummy-image.jpeg"
              fileName: "test-image"
          expect:
            - statusCode: 400  # Bad request
            - hasProperty: "message"
      - think: 1
      - log: "Sending POST request with incorrect AUTH_TOKEN"
        post:
          url: "/pet/{{ petId }}/uploadImage"
          headers:
            Authorization: "incorrect-token"
          formData:
            file:
              filePath: "dummy-image.jpeg"
              fileName: "test-image"
            additionalMetadata: "Metadata for Invalid token"
          expect:
            - statusCode: 401  # Unauthorized
            - hasProperty: "message"
