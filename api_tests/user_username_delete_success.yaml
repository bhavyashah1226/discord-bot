# Test generated by RoostGPT for test roostPostmanTest using AI Type Azure Open AI and AI Model roost-gpt4-32k

# RoostTestHash=d8125b8dc9

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 10
  plugins:
    expect: {}
  payload:
    path: "user_username_delete_success.csv"
    fields:
      - "username"
    skipHeader: true

scenarios:
  - name: "Delete User Scenario"
    flow:
      - log: "Sending delete user request for {{ username }}"
      - delete: 
          url: "/user/{{ username }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "message"
          # After sending the delete request and expecting a 200 status and a message property in the response,
          # it indicates that the user deletion was successful.
      - think: 3
