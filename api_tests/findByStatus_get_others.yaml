# Test generated by RoostGPT for test roostPostmanTest using AI Type Azure Open AI and AI Model roost-gpt4-32k

# RoostTestHash=8a6351bbc5

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "findByStatus_get_others.csv"
    fields:
      - "status"
    skipHeader: true
  headers:
    Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
    
scenarios:
  - name: "findByStatus error tests"
    flow:
      - log: "Request with 'status' as a number"
        get:
          url: "/pet/findByStatus?status=100"
          headers:
            Content-Type: "application/json"
          capture:
            header: "Content-Type"
            json: "$"
          expect:
            - hasProperty: "message"
            - statusCode: 400
            - equals:
                - "{{ contentType }}"
                - "application/json"

      - think: 1

      - log: "Request with invalid 'status' value"
        get:
          url: "/pet/findByStatus?status=invalid"
          headers:
            Content-Type: "application/json"
          capture:
            header: "Content-Type"
            json: "$"
          expect:
            - hasProperty: "message"
            - statusCode: 400
            - equals:
                - "{{ contentType }}"
                - "application/json"

      - think: 1

      - log: "Request with missing 'status' parameter"
        get:
          url: "/pet/findByStatus"
          headers:
            Content-Type: "application/json"
          capture:
            header: "Content-Type"
            json: "$"
          expect:
            - hasProperty: "message"
            - statusCode: 400
            - equals:
                - "{{ contentType }}"
                - "application/json"

      - think: 1

      - log: "Request with invalid URL"
        get:
          url: "/invalidUrl"
          headers:
            Content-Type: "application/json"
          capture:
            header: "Content-Type"
            json: "$"
          expect:
            - statusCode: 404

      - think: 1

      - log: "Request with invalid authorization token"
        get:
          url: "/pet/findByStatus?status=pending"
          headers:
            Content-Type: "application/json"
            Authorization: "Invalid token"
          capture:
            header: "Content-Type"
            json: "$"
          expect:
            - hasProperty: "message"
            - statusCode: 401
            - equals:
                - "{{ contentType }}"
                - "application/json"
