# Test generated by RoostGPT for test roostPostmanTest using AI Type Azure Open AI and AI Model roost-gpt4-32k

# RoostTestHash=fcaadbca4d

config:
  target: "{{ $processEnvironment.API_HOST }}"
  # The expect plugin is enabled.
  plugins:
    expect: {}
  # Declaration of the payload with CSV file
  payload:
    path: "user_post_others.csv"
    fields:
      - "id"
      - "username"
      - "firstName"
      - "lastName"
      - "email"
      - "password"
      - "phone"
      - "userStatus"
    skipHeader: true

scenarios:
  - name: "Failure and edge-case API scenario"
    flow:
      # context
      - think: 5
      # Invalid Authorization
      - log: "Invalid authorization request"
      - post:
          url: "/user"
          headers: 
            Authorization: "INVALID_TOKEN"
          json:
            id: "{{ id }}"
            username: "{{ username }}"
            firstName: "{{ firstName }}"
            lastName: "{{ lastName }}"
            email: "{{ email }}"
            password: "{{ password }}"
            phone: "{{ phone }}"
            userStatus: "{{ userStatus }}"
          expect:
            - statusCode: 401
            - hasProperty: "message"
      - think: 5
      # Invalid URL
      - log: "Invalid URL request"
      - post:
          url: "/invalid-url"
          headers: 
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            id: "{{ id }}"
            username: "{{ username }}"
            firstName: "{{ firstName }}"
            lastName: "{{ lastName }}"
            email: "{{ email }}"
            password: "{{ password }}"
            phone: "{{ phone }}"
            userStatus: "{{ userStatus }}"
          expect:
            - statusCode: 404
            - hasProperty: "error"
      - think: 5
      # Invalid Payload
      - log: "Invalid payload request"
      - post:
          url: "/user"
          headers: 
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            INVALID_KEY: "INVALID_VALUE"
          expect:
            - statusCode: 400
            - hasProperty: "error"
      - think: 5
