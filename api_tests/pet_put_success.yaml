# Test generated by RoostGPT for test roostPostmanTest using AI Type Azure Open AI and AI Model roost-gpt4-32k

# RoostTestHash=a21296b4f6

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "pet_put_success.csv"
    fields:
      - "id"
      - "name"
      - "photoUrls"
      - "tags"
      - "status"
    skipHeader: true

scenarios:
  - name: "Update an existing pet"
    flow:
      - post:
          url: "/pet"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            Content-Type: "application/json"
          json:
            id: "{{ id }}"
            name: "{{ name }}"
            photoUrls: "{{ photoUrls }}"
            tags: "{{ tags }}"
            status: "{{ status }}"
          expect:
            - statusCode: 200
          beforeRequest:
            log: "POST /pet Request sent"
          afterResponse:
            log: "POST /pet Response received"          
        think: 1
