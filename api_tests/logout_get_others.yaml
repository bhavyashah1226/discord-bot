# Test generated by RoostGPT for test roostPostmanTest using AI Type Azure Open AI and AI Model roost-gpt4-32k

# RoostTestHash=a64e3feb39

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  phases:
    - duration: 60
      arrivalRate: 10
  payload:
    path: "logout_get_others.csv"
    fields: ["username", "password"]
    skipHeader: true
scenarios:
  - name: "API tests for /user/logout"
    flow:
      - log: "Calling logout API with invalid headers"
        post:
          url: "/user/logout"
          headers:
            Authorization: "INVALID_TOKEN"
            Content-Type: "application/json"
          expect:
            - statusCode: 401
            - hasProperty: "message"
            - equals: 
                message: "Invalid token"
        # This request does not contain any payload, which is expected to fail
      - think: 5
      - log: "Calling logout API without Authorization"
        post:
          url: "/user/logout"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 401
            - hasProperty: "message"
            - equals: 
                message: "Missing Authorization Header"
        #logout is expected to fail without authorization header
      - think: 5
      - log: "Calling logout API with incorrect url"
        post:
          url: "/invalid/url"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            Content-Type: "application/json"
          expect:
            - statusCode: 404
            - hasProperty: "message"
            - equals: 
                message: "Invalid URL"
        # This request contains incorrect url, which is expected to fail
