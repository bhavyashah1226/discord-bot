# Test generated by RoostGPT for test roostPostmanTest using AI Type Azure Open AI and AI Model roost-gpt4-32k

# RoostTestHash=474b7881ac

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  plugins:
    expect: {}
  payload:
    path: "login_get_success.csv"
    fields: ["username", "password"]
    skipHeader: true

scenarios:
  - name: "API scenarios"
    flow:
      - get:
          url: "/user/login"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          qs:
            username: "{{ username }}"
            password: "{{ password }}"
          expect:
            - statusCode: 200
            - hasProperty: "X-Rate-Limit"
            - hasProperty: "X-Expires-After"
          captures:
            - json: "$"
          log: "Testing the login API with valid credentials and expect the status code 200"
        think: 1
      - get:
          url: "/user/login"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          qs:
            username: "invalid"
            password: "password"
          expect:
            - statusCode: 400
          log: "Testing the login API with invalid username and expect the status code 400"
        think: 1
      - get:
          url: "/user/login"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          qs:
            username: "username"
            password: "invalid"
          expect:
            - statusCode: 400
          log: "Testing the login API with invalid password and expect the status code 400"
        think: 1
