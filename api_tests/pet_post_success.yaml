# Test generated by RoostGPT for test roostPostmanTest using AI Type Azure Open AI and AI Model roost-gpt4-32k

# RoostTestHash=16cf30f894

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 2
  plugins:
    expect: {}
  payload:
    path: "pet_post_success.csv"
    fields: [ "id", "name", "photoUrls", "status", "category_id", "category_name", "tag_id", "tag_name" ]
    skipHeader: true
scenarios:
  - name: "API - Add a new pet to the store"
    flow:
      - post:
          url: "/pet"
          json:
            id: "{{ id }}"
            name: "{{ name }}"
            photoUrls: ["{{ photoUrls }}"]
            status: "{{ status }}"
            category:
              id: "{{ category_id }}"
              name: "{{ category_name }}"
            tags:
              - id: "{{ tag_id }}"
                name: "{{ tag_name }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            Content-Type: "application/json"
          capture:
            - json: "$.id"
              as: "petId"
          expect:
            - statusCode: 200
            - hasProperty: "id"
          log: "Adding a new pet to the store"
      - think: 1
